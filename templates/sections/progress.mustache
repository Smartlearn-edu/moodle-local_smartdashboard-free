<!-- Progress Section (Placeholder) -->
                <div id="section-progress" class="dashboard-section d-none">
                    <div class="alert alert-light border">
                        <h4><i class="fa fa-users text-info"></i> {{#str}}section_progress, local_smartdashboard{{/str}}</h4>
                        <p>Student progress analytics will appear here.</p>
                        <!-- We will implement this next -->
                    </div>
                </div>

                <!-- {{#str}}grades_overview, local_smartdashboard{{/str}} Section (Placeholder) -->
                <div id="section-progress-grades" class="dashboard-section d-none">
                    <div class="alert alert-light border">
                        <h4><i class="fa fa-graduation-cap text-info"></i> {{#str}}grades_overview, local_smartdashboard{{/str}}</h4>
                        <p>{{#str}}student_grades_will_appear, local_smartdashboard{{/str}}</p>
                    </div>
                </div>

                <!-- {{#str}}detailed, local_smartdashboard{{/str}} Progress Section -->
                <div id="section-progress-detailed" class="dashboard-section d-none">
                    <div class="card shadow-sm border-0">
                         <div class="card-body">
                             <h4 class="card-title mb-4"><i class="fa fa-user text-primary me-2"></i>{{#str}}detailed, local_smartdashboard{{/str}} Student Progress</h4>
                             <div id="detailed-progress-controls" class="row mb-4">
                                 <div class="col-md-6 mb-3">
                                     <label class="form-label">Select Student</label>
                                     <select id="detailed-student-select" class="form-select">
                                         <option value="">Choose a student...</option>
                                     </select>
                                 </div>
                                 <div class="col-md-6 mb-3">
                                     <label class="form-label">Filter Activity Type</label>
                                     <select id="detailed-activity-filter" class="form-select">
                                         <option value="">All Activities</option>
                                         <option value="assign">Assignments</option>
                                         <option value="quiz">Quizzes</option>
                                         <option value="forum">Forums</option>
                                     </select>
                                 </div>
                             </div>
                             <div id="detailed-progress-content">
                                 <p class="text-muted text-center py-5">Select a student to view details.</p>
                             </div>
                         </div>
                    </div>
                </div>

                <!-- Risk Details Section -->
                <div id="section-progress-risk" class="dashboard-section d-none">
                    <div class="card shadow-sm border-0 animate__animated animate__fadeIn">
                         <div class="card-body">
                             <h4 class="card-title mb-4"><i class="fa fa-exclamation-triangle text-danger me-2"></i>{{#str}}risk, local_smartdashboard{{/str}}</h4>
                             <div id="risk-details-content">
                                 <div class="text-center p-5"><i class="fa fa-spinner fa-spin fa-3x text-muted"></i></div>
                             </div>
                         </div>
                    </div>
                </div>
